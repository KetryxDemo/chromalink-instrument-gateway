{
  "name": "chromalink-instrument-gateway",
  "version": "1.4.0",
  "private": true,
  "description": "Gateway between lab chromatography instruments and the ChromaLink Data Portal",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
